#program to print sub arrays and display the index
def subarraysum(arr,n,sum_):
    cur_sum=arr[0]
    start=0

    i=1
    while i<n:
        while cur_sum >sum_ and start < i-1:
            cur_sum=cur_sum - arr[start]
            start+=1

        if cur_sum == sum_:
            print("sum found between indexes ")
            print("% d and %d " % (start,i-1))
            for u in range(start,i):
                print(arr[u],end=' ')
            return 1

        if i<n:
            cur_sum=cur_sum+arr[i]
        i+=1

    print("no subarray found")
    return 0




arr=[]
arr=list(map(int,input().split()))
n=len(arr)
sum_=23
subarraysum(arr,n,sum_)
